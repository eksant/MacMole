# ── macOS system files ────────────────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# ── Editor / IDE ──────────────────────────────────────────────────────────────
*~
*.swp
*.swo
.idea/
.vscode/*.code-workspace
.vscode/settings.json

# ── Logs & temp ───────────────────────────────────────────────────────────────
*.log
logs/
tmp/
temp/
*.tmp
*.temp
*.dmg
*.cache
.cache/

# ── Backup files ──────────────────────────────────────────────────────────────
*.bak
*.backup

# ── AI assistant — commands/ and agents/ are TRACKED (project skills) ─────────
.claude/settings.json
.claude/settings.local.json
.claude/projects/
.claude/history/
.claude/statsig/
.claude/todos/
.claude/cache/
# Other AI tooling (fully ignored)
.agents/
.gemini/
.kiro/
GEMINI.md
ANTIGRAVITY.md
WARP.md
AGENTS.md
journal/
.cursorrules
mole_guidelines.md

# ── Wails desktop app — build artifacts ───────────────────────────────────────
# Compiled output
build/bin/
build/darwin/
build/windows/
build/linux/
# Wails dev mode binary compiled at repo root
desktop
MacMole
# Frontend build output & dependencies
frontend/node_modules/
# Ignore dist contents but keep the stub .gitkeep so //go:embed compiles without a frontend build
frontend/dist/*
!frontend/dist/.gitkeep
# NOTE: build/appicon.png, build/trayicon.png, build/trayicon@2x.png
#       are intentionally TRACKED — required by //go:embed in tray_darwin.go

# ── Go build artifacts ────────────────────────────────────────────────────────
*.out
coverage.out
coverage.html
*.exe
*.test

# ── Mole CLI — compiled binaries (source and scripts stay tracked) ─────────────
mole/cmd/analyze/analyze
mole/cmd/status/status
mole/bin/analyze-go
mole/bin/status-go
mole/bin/analyze-darwin-*
mole/bin/status-darwin-*
mole/bin/mole-darwin-*
mole/mo
mole/mole
mole/tests/tmp-*/
mole/tests/*.tmp
mole/tests/*.log

# ── Misc project artifacts ────────────────────────────────────────────────────
session.json
run_tests.ps1
